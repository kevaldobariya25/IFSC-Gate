<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="/style.css">
    <title>IFSC Gate</title>
</head>
<body class="background-img">
<!-- Creating form -->
    <div class="top">
        <form action="/submit" method="post">
            <label for="code"><h1>IFSC Code:</h1></label>
            <input type="text" name="code" required>
            <input type="submit" value="SUBMIT" id="submit" class="btn btn-light">
        </form>
    </div>

<!-- Creating container for details-->    
    <div class="info">
        <% if(locals.detail){ %>
            <p>IFSC: <%= detail.IFSC %></p>
            <p>Bank: <%= detail.BANK %></p>
            <p>Branch: <%= detail.BRANCH %></p>
            <p>Address: <%= detail.ADDRESS %></p>
            <p>Contact: <%= detail.CONTACT %></p>
            <p>City: <%= detail.CITY %></p>
            <p>State: <%= detail.STATE %></p>
            <p>MICR: <%= detail.MICR %></p>
        <% }else if(locals.err){ %>
            <p><%= err %></p>
        <%} else{ %>
            <p><%= content %></p>
        <% } %>
    </div>

<!-- Creating Footer -->
<footer>@KevalDobariya</footer>
</body>
</html>